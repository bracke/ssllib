# Invariant registry

Each entry states a property, names the code that owns it, and names the
verification that establishes it. `ssllib_tools verify` fails a build in which an
entry declares `verification: none`, so an invariant cannot be added without
coverage and cannot lose its coverage silently.

Families: `REC-*` record layer, `TLS13-*` TLS 1.3, `TLS12-*` TLS 1.2, `CERT-*`
certificates and trust, `SES-*` sessions, `EXT-*` extensions, `ERR-*` errors and
alerts, `API-*` API rules, `CONC-*` concurrency, `REP-*` repository.

Coverage kinds: `test` an automated check in the suite; `structure` a property held
by the shape of the type or the absence of an operation, with a test that would
catch a regression; `audit` a check in `ssllib_tools verify`; `proof` a GNATprove
obligation; `interop` an external-stack check. `pending` means the invariant is
declared and its subject is not yet implemented — those are listed in the second
table so that the first table contains only invariants that hold today.

## Invariants established in this release

| ID | Statement | Owner | Verification | Impact if broken |
|---|---|---|---|---|
| REC-1 | No (key, nonce) pair is used twice on a connection. A nonce is a function of the static IV and the sequence number; the sequence advances only after success; it returns to zero only when a new key is installed. | `SSL.Records.Traffic_State` | structure + test: `records: sequence and nonce uniqueness` (identical plaintext under one key yields different ciphertext); `records: every bit flip refused` (a failed open does not advance the sequence) | Confidentiality of the affected records is lost outright |
| REC-2 | No plaintext is exposed before authentication. A record whose tag does not verify produces a zeroed buffer and a zero length. | `SSL.Records.Open`, `SSL.Crypto.Open` | test: `records: no unauthenticated plaintext` (the output is scanned for any plaintext octet and for any non-zero octet) | An attacker can inject data the application treats as authentic |
| REC-3 | The five-octet record header is the AEAD's associated data, so no header field can be altered undetectably. | `SSL.Records.Protect`, `SSL.Records.Open` | test: `records: header is associated data` (each of the five octets is flipped in turn) | The length and type fields become forgeable |
| REC-4 | Padding is removed only after the tag has verified, and a bad tag, bad padding, an invalid inner type and a missing inner type are indistinguishable. | `SSL.Records.Open`, `SSL.Errors.Classify` | test: `records: padding removed after auth`, `records: missing inner type refused`; structure: all four map to `Code_Record_*` with `Restricted` disclosure | A padding oracle |
| REC-5 | Wire octets are never converted into an Ada record by unchecked conversion or by overlay; the header is parsed field by field. | `SSL.Records.Parse_Header`, `SSL.Wire` | structure + test: `records: explicit header codec`; audit: no `Unchecked_Conversion` in `src/` | Representation-dependent parsing; undefined behaviour across targets |
| REC-6 | A record layer sequence number is never wrapped; at the ceiling the endpoint refuses to protect. | `SSL.Records.Protect` | structure: the refusal precedes any AEAD call; test: `records: sequence and nonce uniqueness` | Nonce reuse |
| TLS13-1 | The transcript is the exact octets of each handshake message — type, three-octet length, body — with no record framing and no re-encoding. | `SSL.Transcripts` | test: `transcript: non-finalizing snapshot` (the transcript equals the hash of the concatenated messages) | The two ends derive different keys; a tampered message may go undetected |
| TLS13-2 | A transcript snapshot does not finalize; secrets may be derived at several milestones and hashing continues. | `SSL.Transcripts.Hash`, `SSL.Crypto.Snapshot` | test: `transcript: non-finalizing snapshot` (two consecutive snapshots agree, and absorbing afterwards changes the hash) | Every derivation after the first is wrong |
| TLS13-3 | After a HelloRetryRequest the transcript is the synthetic `message_hash` form of RFC 8446 section 4.4.1, applied at most once. | `SSL.Transcripts.Apply_Hello_Retry_Transform` | test: `transcript: message_hash transform` (compared against the hash computed independently); structure: `Transformed` gates the precondition | Retry handshakes fail against every conforming peer |
| TLS13-4 | The key schedule agrees with RFC 8446 section 7.1, and each stage is derived once and in order. | `SSL.Key_Schedule` | test: `key schedule: RFC 8448 section 3` (authoritative vectors, plus an internal-consistency cross-check); `key schedule: stage ordering` | Handshakes fail, or succeed on keys the peer did not derive |
| TLS13-5 | There is no getter for a raw key-schedule stage secret; only sanctioned products are reachable. | `SSL.Key_Schedule` | structure: the package exports no stage-secret accessor; audit: the package is a private child, so no application can name it | A caller can derive material the schedule did not sanction |
| TLS13-6 | The early secret and the binder key are scrubbed the moment the handshake stage is reached. | `SSL.Key_Schedule.Derive_Handshake` | structure + test: `key schedule: stage ordering` (a wiped schedule reports the unstarted state) | Loss of intra-connection forward secrecy |
| TLS13-7 | A KeyUpdate advances a direction's traffic secret and scrubs the retired one; the two directions advance independently. | `SSL.Key_Schedule.Advance_Traffic_Secret` | test: `key schedule: KeyUpdate advance` (key and IV both change; the other direction's generation does not) | The update provides no forward secrecy, or desynchronizes the directions |
| TLS13-8 | Exporter output is bound to its label and to the context value, and is deterministic: the two ends of one connection derive the same material under one label, and a different label gives different material. Under TLS 1.3 an absent context and an empty one coincide, by RFC 8446 section 7.5's own definition. | `SSL.Key_Schedule.Export` | test: `exporters: label and context binding`; `post-handshake: exporters, channel bindings, KeyUpdate` (both ends agree; a different label differs) | Two callers asking for different keys receive the same key |
| TLS13-9 | Each ticket's PSK is bound to that ticket's nonce, so several tickets from one connection yield unrelated PSKs. | `SSL.Key_Schedule.Resumption_PSK` | test: `tickets: per-nonce PSK separation` | Using one ticket compromises the others |
| TLS13-10 | The octets a CertificateVerify signs are 64 spaces, the role's own context string, a zero octet, and the transcript hash -- so a client's signature can never be replayed as a server's. | `SSL.Handshake_Messages.Certificate_Verify_Content` | test: `handshake: CertificateVerify signed content` (both roles built, prefix and separator checked, the two compared) | A signature made in one role is accepted in the other |
| TLS13-11 | A NewSessionTicket lifetime beyond the seven days RFC 8446 section 4.6.1 permits is refused, never clamped; a ticket of zero octets is refused. | `SSL.Handshake_Messages.Parse_New_Session_Ticket` | test: `handshake: CertificateVerify, Finished, KeyUpdate, tickets` | The two ends disagree about when a ticket died, and the disagreement surfaces as an unexplained resumption failure |
| TLS13-12 | The order in which traffic keys are installed is decided in one place and reported as an ordered plan; a driver never infers it. A client installs both handshake directions after ServerHello and its application read keys only after the server's Finished has verified. | `SSL.TLS13`, `SSL.TLS13.Client`, `SSL.TLS13.Server` | test: `handshake: TLS 1.3 client against TLS 1.3 server, end to end` (the plan is recorded and the ordering asserted; a record sealed by the client opens on the server) | The two ends encrypt under different epochs, or plaintext goes out under a key that is not yet installed |
| TLS13-13 | Every choice a server makes is checked against what this client actually offered -- suite, version, group, application protocol, legacy session identifier -- and a second HelloRetryRequest, or one asking for a group whose share was already supplied, is refused. | `SSL.TLS13.Client` | test: `handshake: TLS 1.3 client against TLS 1.3 server, end to end`; structure: each refusal precedes any use of the value | A rewritten ClientHello is answered as though it were this client's |
| TLS13-14 | A peer's certificate is validated in full before its CertificateVerify is checked, and its Finished is checked after both. | `SSL.TLS13.Client`, `SSL.TLS13.Server` | structure: the state machine reaches `Wait_Certificate_Verify` only through a successful validation, and `Wait_Finished` only through a successful signature check; test: the end-to-end handshake exercises the whole order | A signature is verified under a key from a certificate this endpoint has not accepted |
| EXT-1 | An extension block refuses a duplicate extension, and refuses it before the second body is opened. | `SSL.Extensions.Next` | test: `extensions: duplicates and contexts` | A peer shows one value to a checker and another to the state machine |
| TLS12-7 | One ClientHello serves both versions: when the policy offers TLS 1.2 the hello also carries extended_master_secret, an empty renegotiation_info and the uncompressed point format. A client that had to send a second hello would give a downgrade attacker the extra round trip it wants. | `SSL.Handshake_Messages.Encode_Client_Hello` | test: `connections: a TLS 1.2-only client negotiates down` | A version change costs a whole extra handshake |
| TLS12-8 | The version is decided by supported_versions alone -- on the client by whether the ServerHello carries it, on the server by whether the ClientHello offers TLS 1.3 -- and a server never chooses TLS 1.2 when the client offered TLS 1.3. | `SSL.Engines.Should_Fall_Back`, `SSL.Engines.Should_Serve_Legacy` | test: `connections: a TLS 1.2-only client negotiates down`; structure: neither reads the legacy version field | A downgrade succeeds because the legacy field was believed |
| TLS12-9 | A client falling back adopts the ClientHello it already sent rather than encoding a second one, so the transcript both ends hash is the same octets. | `SSL.TLS12.Client.Adopt_Hello`, `SSL.Engines.Fall_Back` | test: `connections: a TLS 1.2-only client negotiates down` (a ServerKeyExchange signature verifies, which it cannot if the randoms differ) | Every Finished and every ServerKeyExchange signature fails |
| TLS12-1 | Restricted TLS 1.2 negotiates ECDHE and an AEAD suite and nothing else: no static RSA, no static or anonymous DH, no CBC, no compression. | `SSL.Cipher_Suites`, `SSL.TLS12.Client`, `SSL.TLS12.Server` | structure: the suite registry holds only ECDHE-AEAD suites, and both machines refuse anything else; test: `handshake: restricted TLS 1.2, client against server` | No forward secrecy, or a padding oracle |
| TLS12-2 | Extended master secret is mandatory in both roles: a peer that does not negotiate it gets no connection, and the master secret is derived from the ClientKeyExchange transcript and never from the two randoms. | `SSL.TLS12.Derive_Extended_Master`, both machines | structure: the RFC 5246 derivation is not implemented at all; test: `handshake: restricted TLS 1.2, client against server` | The triple-handshake attack: two connections made to share one master secret |
| TLS12-3 | A ServerKeyExchange signature covers the two randoms and the *exact* parameter octets that arrived, never a re-encoding. | `SSL.TLS12.Key_Exchange_Signed_Content`, `SSL.TLS12.Messages.Parameters_Span` | structure: the parser reports a span and the verifier uses it; test: `handshake: restricted TLS 1.2, client against server` | A signature verifies over something the peer did not sign, and an ephemeral share can be substituted |
| TLS12-4 | The TLS 1.2 record construction is implemented as its own, not as a variation of TLS 1.3's: the real content type travels in the clear, the additional data carries the plaintext length, and the GCM nonce is four fixed octets plus eight explicit ones -- except ChaCha20-Poly1305, which RFC 7905 gave the TLS 1.3 construction. | `SSL.TLS12.Records` | test: `handshake: restricted TLS 1.2, client against server` (records cross in both directions; a flipped tag bit is refused and yields no plaintext) | Records no peer can read, or a nonce reused |
| TLS12-5 | The key block is split in the RFC 5246 section 6.3 order and each direction gets its own half; a ChangeCipherSpec is the epoch switch and the record after it uses the new keys. | `SSL.TLS12.Derive_Key_Block`, both machines | test: `handshake: restricted TLS 1.2, client against server` (both directions are exercised, which is what catches a swapped split) | Each end encrypts with the other's key, or a Finished goes out in the clear |
| TLS12-6 | Renegotiation is refused in both roles, and only the uncompressed elliptic point format is accepted. | `SSL.TLS12.Client`, `SSL.TLS12.Server` | structure: a non-empty `renegotiation_info` and a point-format list without the uncompressed form are each a named refusal | The renegotiation attack, or arithmetic on an attacker-chosen compressed point |
| TLS13-15 | A KeyUpdate message is protected under the key it replaces, and the new key is installed only after the message is queued. Both directions keep working across the change. | `SSL.Engines.Send_Key_Update`, `SSL.Engines.Receive_Key_Update` | test: `post-handshake: exporters, channel bindings, KeyUpdate` (generations advance on both ends, and data flows both ways afterwards) | The peer receives a message under a key it does not have, and reads it as a forgery |
| TLS13-16 | An `update_requested` KeyUpdate is answered with `update_not_requested`, and the run of peer updates is bounded. | `SSL.Engines.Receive_Key_Update` | test: `post-handshake: exporters, channel bindings, KeyUpdate`; structure: the answer's flag is a literal, not the received one | An endless exchange of updates, or a flood that costs the peer four octets and this endpoint a derivation |
| TLS13-17 | A `tls-exporter` channel binding agrees between the two ends; a `tls-server-end-point` binding is the peer's certificate digest and is refused when no certificate was presented. `tls-unique` does not exist here. | `SSL.Channel_Bindings` | test: `post-handshake: exporters, channel bindings, KeyUpdate` (both ends agree; an anonymous peer is refused); structure: no `tls-unique` operation is declared | An authentication exchange is relayed onto another connection |
| EXT-3 | Each extension is accepted only in the messages RFC 8446 section 4.2 permits it in, and the two features this library declines are permitted nowhere. | `SSL.Extensions.Permitted` | test: `extensions: RFC 8446 4.2 contexts`, `handshake: ClientHello refusals` (early_data), `handshake: ServerHello and HelloRetry` (a misplaced cookie) | An extension acts in a message where its meaning is undefined |
| EXT-4 | An unrecognized extension identifier is kept for diagnostics and acted on nowhere; it is tolerated only in a ClientHello. | `SSL.Extensions` | test: `extensions: identifiers`, `extensions: duplicates and contexts` | An unsolicited response is treated as solicited |
| EXT-5 | A handshake message's declared body length must equal the octets supplied before any field is read. | `SSL.Handshake_Messages` | test: `handshake: ClientHello truncated at every length` (every prefix refused) | A truncated message parses as a shorter well-formed one |
| EXT-6 | A key share whose length does not match its stated group never reaches key agreement. | `SSL.Handshake_Messages` | test: `handshake: ClientHello refusals` (a four-octet x25519 share is dropped) | Malformed group elements reach curve arithmetic |
| EXT-7 | Every deferred length prefix is back-patched from a variable owned by exactly one nesting level, and a message this library encodes parses back through the same parser a peer would use. | `SSL.Handshake_Messages.Encode_Client_Hello` | test: `handshake: ClientHello encode then parse back` (fixed octets checked directly, then a full round trip) | An outer length is patched with an inner vector's position, and the message a peer receives is not the message the transcript hashed |
| EXT-8 | A ClientHello's `pre_shared_key` must be the last extension in its block, and the offset at which the binders stop covering the message is reported by the parser rather than recomputed. | `SSL.Handshake_Messages.Read_PSK_Offer` | test: `handshake: pre_shared_key offer and the last-extension rule` (an extension after the offer is refused by code; the reported offset is compared against the binders list) | A peer changes an extension the binder does not cover, or a server verifies a binder over the wrong octets |
| ERR-1 | Every alert a peer can observe is chosen by one table; no failure site chooses its own. | `SSL.Errors.Classify` | structure + test: `errors: mapping, disclosure, accumulation`; audit: `SSL.Alerts.Local_Alert` is called only from `SSL.Errors` | The alert surface becomes an oracle nobody can review |
| API-1 | Every engine and connection operation is partial: it reports what it actually consumed or produced, and zero is an ordinary answer rather than a failure. | `SSL.Engines`, `SSL.Connections` | test: `connections: handshake and bulk data over partial I/O` (a transport that refuses half its reads and accepts 97 octets per write completes a handshake and a 40 kB message) | Non-blocking callers cannot use the library, or lose octets at every short transfer |
| API-2 | A transport is application code and is called only through a boundary that converts an exception, an over-long count, or a count on a non-Ok status into a structured failure. | `SSL.Transports.Receive_Safely`, `SSL.Transports.Send_Safely` | structure: no other call site invokes `Receive`/`Send`; the boundary zeroes the buffer and the count on every failure path | An exception unwinds a connection holding traffic keys, or uninitialized buffer contents reach the record layer |
| API-3 | A stream that ends without a close_notify is reported as a truncation and never as an orderly close. | `SSL.Engines.Report_End_Of_Stream` | test: `connections: a stream ending without close_notify` | An attacker who can cut a connection makes a partial response look complete |
| API-4 | Alerts a peer can observe are chosen by the error table alone; the engine looks the alert up and never picks one. | `SSL.Engines.Fail` | structure: the only alert construction on a failure path is `SSL.Errors.Alert_Of`; audit: `SSL.Alerts.Local_Alert` is called only from `SSL.Errors` | The alert surface becomes an oracle nobody can review |
| API-5 | Application data is refused before the handshake completes and after a close_notify in either direction, with the specific reason rather than a generic one. | `SSL.Engines.Write_Plaintext`, `SSL.Engines.Dispatch_Content` | test: `engines: two engines, handshake, data both ways, shutdown` (a write after close_notify is refused by code) | Unauthenticated data is delivered as though it were authenticated |
| ERR-2 | The first terminal failure is preserved and never displaced; later failures are counted, bounded. | `SSL.Errors.Failure_Record` | test: `errors: mapping, disclosure, accumulation` | The reported cause is a consequence rather than the cause |
| ERR-3 | A `Restricted` failure renders as its category and code only, with parameters withheld even from a local log. | `SSL.Errors.Image` | test: `errors: mapping, disclosure, accumulation` (the rendering is scanned for the parameter's text) | Shipped logs disclose which check a forgery failed |
| ERR-4 | Terminality is decided on the alert description, never on the peer's level octet; an unrecognized description is terminal and its number is preserved. | `SSL.Alerts.Is_Terminal`, `SSL.Alerts.Peer_Alert` | test: `alerts: values, terminality, unknown peer alerts` | A peer talks the endpoint into continuing after a fatal condition |
| ERR-5 | Numeric error codes and alert values are written out, never derived from enumeration positions. | `SSL.Errors`, `SSL.Alerts` | structure + test: `alerts: values, terminality, unknown peer alerts` (wire values asserted individually) | Reordering a declaration silently changes the wire or the logs |
| ERR-6 | An unusable ticket is non-fatal, sends no alert, and does not disclose why. | `SSL.Errors.Classify` | test: `errors: mapping, disclosure, accumulation` | Ticket handling becomes a forgery oracle |
| ERR-7 | Diagnostics are off unless a configuration attaches a sink; nothing here reads an environment variable, and nothing opens a file. | `SSL.Diagnostics`, `SSL.Configurations.Set_Diagnostics` | structure: the default level is `Off` with a null sink; audit: no environment access anywhere in `src/`; test: `diagnostics: levels, redaction, bounded facts, a sink that raises` | A library writes into an application's output uninvited, or an attacker who can set environment variables turns logging on |
| ERR-8 | An event carries at most four bounded named facts, and a diagnostic sink that raises loses its event and nothing else. | `SSL.Diagnostics.Add`, `SSL.Diagnostics.Emit_Safely` | test: `diagnostics: levels, redaction, bounded facts, a sink that raises` | A hostile peer drives an unbounded allocation, or an application breaks its own connections by writing a bad logger |
| ERR-9 | Key logging is reachable only by naming `SSL.Unsafe`, is off unless a configuration attaches a sink, opens no file, honours no environment variable, and wipes its formatting buffer on every path. | `SSL.Unsafe.Key_Logging` | test: `key logging: standard labels, line format, a sink that raises`; structure: the buffer is scrubbed before every return, including the one where the sink raised | A debugging facility becomes a production vulnerability that decrypts every connection |
| CERT-6 | A peer's key share is validated before any curve or modular arithmetic runs: length against the group's stated width always, then the family's own rule -- on-curve for the NIST curves, non-zero output for X25519, 1 < Y < p-1 and a shared secret that is not 1 or p-1 for the finite-field groups. | `SSL.Crypto.Agree`, `SSL.Supported_Groups.Share_Length` | test: `agreement: every offered group, both directions` (wrong-length and all-zero shares refused over each group) | An attacker forces a known or small-order shared secret |
| CERT-7 | An RSASSA-PSS handshake signature is verified under exactly the parameters its scheme fixes: MGF1 with the same hash, salt length equal to the digest length. | `SSL.Crypto.Verify_Signature` | test: `crypto: RSASSA-PSS verification` (a known-good signature verifies, the same signature fails under the wrong hash, a flipped bit is refused) | A signature verifies under parameters the signer did not use |
| CERT-1 | The validation pipeline runs in one fixed order: decode, parse, bounded path build, path validation, purpose, key usage, identity -- with revocation and pinning run by the caller after it, in that order. | `SSL.Certificate_Validation.Validate` | structure: each step returns before the next begins, and the two the caller runs need evidence this package deliberately does not fetch | Revocation is judged on an untrusted issuer's assertion; identity substitutes for a path |
| CERT-9 | A stapled OCSP response is read only through CryptoLib, and only after the chain it is about has validated; nothing is ever fetched. | `SSL.Certificate_Validation.Check_Stapled_Status`, `SSL.TLS13.Client` | structure: the check runs after `Validate` and before pinning, and the only OCSP decode in `src/` is CryptoLib's; audit: no network access anywhere in `src/` | Revocation is judged on an untrusted issuer's assertion, or a handshake leaks who is connecting to whom |
| CERT-10 | Without a wall clock, a stapled response reports stale rather than affirmative. | `SSL.Certificate_Validation.Check_Stapled_Status` | structure: the clock check precedes the decode | A signed "not revoked" from years ago reads as current |
| SES-7 | A client's binder is computed over its ClientHello truncated immediately before the binders list, and a server verifies it over the same span in constant time. | `SSL.TLS13.Client.Write_Client_Hello`, `SSL.TLS13.Server.Consider_Offer` | test: `sessions: a second connection resumes from the first one's ticket` (three connections; the second and third resume) | Resumption fails against every conforming peer, or a stolen ticket resumes without its key |
| SES-8 | A ticket that opens but whose binder does not verify ends the connection; every other reason a ticket is unusable falls back to a full handshake. | `SSL.TLS13.Server.Consider_Offer` | structure: the binder mismatch is the one path that reports a failure rather than declining | A server becomes a free oracle for testing stolen tickets |
| SES-9 | A resumed handshake sends no Certificate and no CertificateVerify, and reports its peer as inheriting an earlier authentication rather than performing one. | `SSL.TLS13.Client`, `SSL.TLS13.Server` | test: `sessions: a second connection resumes from the first one's ticket`; structure: the resumed path returns before the Certificate is written | A resumed connection is reported as freshly authenticated |
| SES-10 | Resumption always comes with a fresh key exchange: a client offers `psk_dhe_ke` alone, and a server declines an offer that does not permit it. | `SSL.Handshake_Messages.Encode_Client_Hello`, `SSL.TLS13.Server.Consider_Offer` | structure: the mode octet is a literal; the server's check precedes any ticket work | A resumed connection has no forward secrecy |
| SES-1 | A session ticket is authenticated before it is parsed, and its version, protocol family, purpose and key identifier are part of what is authenticated. | `SSL.Ticket_Keys.Open` | structure: the AEAD open precedes the decoder and the header is the additional data; test: `sessions: key ring states, ticket issue, cache bindings` | Chosen bytes reach the ticket decoder, or a format change is rolled back |
| SES-2 | Every reason a ticket is unusable -- unknown key, wrong version, bad tag, expired -- produces one undifferentiated refusal and a full handshake. | `SSL.Ticket_Keys.Open` | structure: a single `Refuse` on every path; test: `sessions: key ring states, ticket issue, cache bindings` | A peer probes a server's key rotation by the refusals it gets |
| SES-3 | Rotating a ticket key demotes the previous one to decrypt-only rather than retiring it, so tickets already issued keep working. | `SSL.Ticket_Keys.Rotate` | test: `sessions: key ring states, ticket issue, cache bindings` (a second rotation leaves two keys) | A routine rotation becomes a thundering herd of full handshakes |
| SES-4 | A session is offered only where every binding matches: server name, security context, configuration fingerprint, trust fingerprint, and liveness. | `SSL.Sessions.Matches` | test: `sessions: key ring states, ticket issue, cache bindings` (a different context, a different name and an expired instant are each refused) | A session resumes into a connection the application did not ask for |
| SES-5 | Each ticket's pre-shared key is derived under its own nonce, and a session handed out of the cache is removed from it. | `SSL.Engines.Issue_Ticket`, `SSL.Sessions.Client_Caches.Memory` | test: `sessions: key ring states, ticket issue, cache bindings` (the cache empties when a session is taken); `tickets: per-nonce PSK separation` | Offering one ticket offers all of them, or one offered twice makes two connections linkable |
| SES-6 | Ticket issuance is refused at `Build` until a ticket-key ring is attached. | `SSL.Configurations.Build` | test: `sessions: key ring states, ticket issue, cache bindings` (the refusal is asserted by code) | A server issues tickets nobody can decrypt |
| CERT-8 | A Certificate message is bounded three ways before any entry is read -- whole message, each certificate, and how many there may be -- and a zero-length entry never reaches a DER parser. A stapled response belongs to the entry that carries it. | `SSL.Handshake_Messages.Parse_Certificate` | test: `handshake: Certificate round trip, staples and bounds` (a chain past the configured count refused, a zero-length entry refused, the leaf's staple not attributed to the issuer) | An unauthenticated peer chooses how much this endpoint holds; a staple is credited to a certificate it does not describe |
| CERT-2 | Identity matching uses subjectAltName only, with no Common Name fallback and no configuration that adds one. | `SSL.Certificate_Validation` | structure: `Allow_Common_Name_Fallback` is False and is not a parameter | A free-text display field is parsed as a hostname |
| CERT-3 | A private key never leaves a credential: no operation returns it or any parameter of it. | `SSL.Credentials` | structure: the package exports no such operation; the key is a private component of a limited controlled type | Key material reaches a caller that does not scrub |
| CERT-4 | A required trust source that is unavailable or empty fails closed, and a peer's self-signed certificate is never an implicit anchor. | `SSL.Trust`, `SSL.Certificate_Validation` | test: `trust: snapshots fail closed when empty`; structure: `Is_Trust_Anchor` answers on provenance, never on self-signature | A misconfiguration becomes an unauthenticated connection |
| CERT-5 | Pinning never silently disables identity matching, and a pin set that covers nothing is a failure rather than a pass. | `SSL.Trust.Pinning` | test: `pinning: scope, period, and what it refuses` | A pinned certificate issued for another name is accepted for this one |
| CERT-10 | A revocation always fails, in every policy mode including the disabled one. | `SSL.Trust.Revocation.Evaluate` | test: `revocation: policy and evidence` (checked across every mode) | Disabling the *search* for status is read as ignoring status that arrived |
| CERT-11 | No revocation mode opens a network connection. | `SSL.Trust.Revocation` | structure: the package takes evidence as a parameter and has no transport of any kind | The handshake blocks on, and leaks the destination to, a third party |
| CERT-8 | A server does not acquire finite-field groups except through an operation distinct from the client-side one, so the denial-of-service amplification is chosen and never inherited from a copied group list. | `SSL.Configurations.Set_Groups`, `.Accept_Finite_Field_Groups_With_Amplification_Risk` | structure + test: `configuration: finite-field groups need an explicit opt-in (CERT-8)` (both roles refuse a mixed list through `Set_Groups`; the named acceptor adds them after the curves) | An attacker forces a 62 ms exponentiation per connection attempt at no cost to itself |
| CERT-9 | There is no configuration in which certificate path validation or identity checking is disabled. | `SSL.Configurations` | structure: no such field exists on either builder or configuration; a client with no expected identity is refused at Build | The most dangerous state is reachable from ordinary configuration |
| API-1 | Every declared length from a peer is checked against a configured bound before any storage is reserved. | `SSL.Wire.Open_Vector_*`, `SSL.Limits` | test: `wire: length-prefixed vectors`, `wire: every truncation refused`, `limits: defaults and consistency` | A declared length becomes an allocation |
| API-2 | A parse failure is a sticky flag, never an exception, and a failed cursor yields no values. | `SSL.Wire` | test: `wire: sticky failure flag`, `wire: every truncation refused` | An ordinary protocol error escapes as an unhandled exception |
| API-3 | Buffers never grow. A full queue refuses, all-or-nothing, and is unchanged. | `SSL.Buffers.Queue` | test: `buffers: bounded backpressure`, `buffers: partial append` | An attacker chooses the endpoint's memory use |
| API-4 | Secrets are scrubbed through volatile stores, on overwrite and on finalization, over the whole declared capacity. | `SSL.Secrets`, `SSL.Crypto.Scrub` | test: `secrets: wipe and overwrite` | Key material outlives the connection in freed memory |
| API-10a | A configuration that cannot work is refused at Build with a code naming the problem, not accepted and left to fail at a handshake. | `SSL.Configurations.Build`, `.Validate_Common` | test: `configuration: validation refusals`, `configuration: secure server defaults` (a missing identity, a version with no suite, an unoffered key share, an unsatisfiable policy, ticket issuance with no key) | A configuration fails in production against one peer and nowhere else |
| API-10b | Enabling TLS 1.2 never lets a peer that speaks both be steered off TLS 1.3: no TLS 1.2 suite may precede a TLS 1.3 suite in preference order. | `SSL.Configurations.Validate_Common` | test: `configuration: compatibility does not weaken TLS 1.3`, `configuration: TLS 1.2 suite ahead of TLS 1.3 refused` | Modern-compatibility policy silently downgrades every connection |
| API-10c | Two policies a peer could tell apart have different configuration fingerprints, so a session cannot be resumed under policy it was not established under. | `SSL.Configurations.Fingerprint` | test: `configuration: fingerprint separation` (identity, resumption setting and security context each change it) | A session crosses a policy or a tenant boundary |
| API-10 | A secret's capacity is stated at its declaration; there is no default, so a secret too small for what it will hold is a compile-time failure. | `SSL.Secrets.Secret` | structure: the discriminant has no default, so an unconstrained declaration does not compile | A 512-octet shared secret silently truncated into a 64-octet buffer |
| API-5 | MAC, tag and secret comparison is constant-time in the contents. | `SSL.Secrets.Equal`, `SSL.Crypto.Equal` | test: `secrets: constant-time equality`; structure: both delegate to `CryptoLib.Constant_Time.Equal` | A timing side channel on tag comparison |
| API-6 | Configured limits that would deadlock rather than refuse are rejected before use, and no configuration Is_Valid accepts can stall: an endpoint never waits for an empty queue to drain. | `SSL.Limits.Is_Valid`, `SSL.Engines.Advance` | test: `limits: defaults and consistency` (each inconsistency checked individually); `a plaintext queue of one record still moves one` (the smallest queue Is_Valid accepts, carrying a full-size record) | A configuration that reports backpressure forever |
| API-11 | The queues an endpoint reserves are the ones it configured. Maximum_Input_Buffer, Maximum_Ciphertext_Queue and Maximum_Plaintext_Queue are the capacities, so the backpressure boundary and the per-connection memory are the caller's decision and what Is_Valid checks is what the engine holds. | `SSL.Engines.Reserve_Buffers`, `SSL.Limits` | test: `a configured plaintext queue is the boundary` (a reader that never reads holds no more than its configured queue) | Configured bounds decide nothing, and every limit checked against them is vacuous |
| API-8 | A connection is parsed incrementally at every octet boundary: a whole handshake and forty kilobytes of application data survive being delivered one octet at a time, and several records supplied in one call are all processed. | `SSL.Engines.Advance` | test: `records: one octet at a time, and several per supply` | A parser assumes a whole record arrived, or a driver leaves records sitting in the buffer for ever |
| API-7 | ALPN protocol names are compared as octets, with no case folding and no UTF-8 assumption. | `SSL.ALPN` | test: `alpn: opaque names, selection, policy` | A protocol is negotiated that the peer did not offer |
| API-8 | DNS name folding is ASCII-only and locale-independent; IP literals are refused as DNS names; non-ASCII names are refused rather than guessed at. | `SSL.Server_Names` | test: `names: normalization, wildcards, addresses` | Hostname comparison depends on the process locale, or can be tricked |
| API-9 | A wildcard matches exactly one label, leftmost only; partial and registry-wide wildcards are refused; an exact match outranks any wildcard. | `SSL.Server_Names` | test: `names: normalization, wildcards, addresses` | A certificate is accepted for names the operator did not intend |
| CONC-2 | A provider callback that raises is caught at the boundary and converted to a structured failure attributed to the provider; it never propagates into a handshake. | `SSL.Credentials.Signers.Sign_Externally`, `.Supports_Safely` | test: `signers: the provider boundary contains every misbehaviour` (raising, refusing, and claiming an over-long signature) | An application bug unwinds through a state machine holding traffic keys |
| CERT-12 | A server presents the credential that covers the requested name most specifically, then one that can produce an offered scheme under the negotiated version, with insertion order as the last tie-break. | `SSL.Configurations.Select_Credential`, `SSL.Credentials.Covers` | test: `credentials: SNI selection by specificity then capability` | Adding a credential silently changes which one an existing name resolves to |
| CERT-13 | A client cannot be configured without trust anchors, and a server cannot be configured without a credential, or with client authentication requested and no anchors to check one against. | `SSL.Configurations.Build` | test: `configuration: secure client defaults`, `configuration: secure server defaults` | A client that verifies nothing, or a server that requests a certificate it never checks |
| REP-1 | No runtime source names CryptoLib outside the two declared seams: `SSL.Crypto` for cryptography, and `SSL.Credentials`/`SSL.Trust`/`SSL.Certificate_Validation` for X.509, plus the two units that reach only for secure wipe and constant-time comparison. | whole of `src/` | audit: `ssllib_tools verify`, against an explicit short list | The ownership boundary becomes unreviewable |
| CONC-1 | The bounded in-memory session cache is safe under concurrent use: eight tasks doing every operation it has, on overlapping keys, leave it consistent and within its capacity. | `SSL.Sessions.Client_Caches.Memory` | test: `concurrency: eight tasks on one session cache` | A torn session, an index out of range, or a cache that outgrows its bound |
| CONC-2 | This library never calls an application sink from inside an application sink. | `SSL.Diagnostics.Emit_Safely`, `SSL.Engines.Note` | test: `callbacks: a sink that calls back into the library` (the observed nesting depth is asserted to be one) | An application doing anything stateful in a callback sees its own state half-updated |
| API-6 | Every handshake parser answers rather than raises, on any input including none: the header parser has no length precondition, because it is the first thing every message from a peer reaches. | `SSL.Handshake_Messages.Parse_Header` | test: `mutation: every damaged message answers rather than raises` (four seeds, every bit flip, truncation, insertion, deletion and corrupted length, through every parser) | A hostile input becomes a raised exception at whichever caller forgot to check |
| API-7 | Each configured bound is checked at exactly the right place, and a declared length is bounded before any storage is reserved for it. | `SSL.Handshake_Messages`, `SSL.Extensions` | test: `limits: each bound at limit-1, limit and limit+1` (plus a sixteen-megabyte declared length behind a four-octet message) | An off-by-one bound, or a peer that names a size and gets it allocated |
| REP-2 | No runtime source names AUnit or `project_tools`. | whole of `src/` | audit: `ssllib_tools verify` | The runtime acquires a test-only dependency |
| REP-3 | Every declared internal check is called from a registered AUnit routine. | `tests/src` | audit: `ssllib_tools verify` | A check passes without testing anything |
| REP-4 | The crate version in `alire.toml` and `SSL.Version.Crate_Version` agree. | `alire.toml`, `SSL.Version` | audit: `ssllib_tools verify` | A release is labelled wrongly |
| REP-5 | Every document the project promises exists and is not empty. | `docs/`, root | audit: `ssllib_tools verify` | A promised document reads as covered when it is not |
| REP-6 | Every invariant in this registry declares verification coverage. | this file | audit: `ssllib_tools verify` | An invariant nobody checks |
| REP-7 | All tooling is Ada driven through `project_tools`; no shell, Python, Make, Perl, Ruby or Node. | `tests/src/tools`, `alire.toml` | audit: no such files in the repository; `ssllib_tools` is the only driver | Workflow logic leaves the language it is meant to be checkable in |

## Invariants declared, subject not yet implemented

These are recorded now so the coverage obligation is on the record before the code
exists. `ssllib_tools verify` does not yet gate on them, because gating on an
invariant whose subject is absent would report a pass for something untested.

| ID | Statement | Planned verification |
|---|---|---|
| TLS13-10 | CertificateVerify signs 64 spaces, the exact context string, a zero separator and the transcript hash. | test against RFC 8448; interop |
| TLS13-11 | A Finished message is appended to the transcript only after it has verified. | test; structure |
| TLS13-12 | An empty client Certificate is permitted only where policy allows, and is never followed by CertificateVerify. | test |
| TLS13-13 | Directional key installation timing matches RFC 8446 exactly. | test; interop |
| TLS13-14 | HelloRetryRequest occurs at most once, and never requests a group already supplied with a valid share. | test |
| TLS12-1 | Extended Master Secret is required; a peer that does not negotiate it is refused, and no non-EMS session is ever stored or resumed. | test; interop |
| TLS12-2 | ChangeCipherSpec is the real epoch switch; malformed, early, repeated or unexpected CCS is refused. | test |
| TLS12-3 | No renegotiation occurs, in either role. | test; interop |
| TLS12-4 | Downgrade sentinels are emitted and legacy downgrade is refused. | test; interop |
| CERT-1 | The validation pipeline runs in order: decode, parse, bounded path build, path validation, purpose, key usage, identity, revocation, pinning, CertificateVerify, Finished. | test |
| SES-1 | A session is bound to its expected identity, endpoint, SNI, ALPN, protocol, suite hash, trust fingerprint, configuration fingerprint, client authentication state and application security context, and is never resumed outside them. | test |
| SES-2 | A ticket is authenticated before it is parsed, and is never a raw Ada record. | test |
| SES-3 | Exactly one active ticket encryption key at a time; retired keys are decrypt-only and bounded. | test |
| EXT-2 | `pre_shared_key` is last in a ClientHello, and its binder covers the partial transcript exactly. | test against RFC 8448 |
| CONC-1 | A base connection is one-task-at-a-time; the synchronized wrapper serializes one reader, one writer and one controller through a single driver. | test |
